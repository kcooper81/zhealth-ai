import Chat from "@/components/Chat";

export const metadata = { title: "Chat — Z-Health" };

export default function ChatPage() {
  return (
    <main className="h-screen overflow-hidden">
      <Chat />
    </main>
  );
}
